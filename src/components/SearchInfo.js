/*import React from 'react';
import { useRef } from 'react';

class SearchInfo extends React.Component {
	this.handleAddFilter(e) {
		const info = inputRef.current.value;
		console.log(info);
		inputRef.current.value = null;
	}
	render() {
		const inputRef = useRef();
		return (
			<div>
				<input id="inputText" type="text" ref={inputRef} />
				<button id="searchBtn" class="btn btn-secondary btn-sm" onClick={handleAddFilter}>
					Search
				</button>
			</div>
		);
	}
}

export default SearchInfo;*/
